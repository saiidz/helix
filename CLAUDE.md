# Claude Code entry point

Read `AGENTS.md` for the project contract. Use `HELIX_ROADMAP.md` for milestones and `STATUS.md` for verified completion. Load only task-relevant documentation. This pointer avoids a second, conflicting set of instructions. Model-provider terms and user authorizations still apply.
