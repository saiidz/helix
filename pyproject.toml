[project]
name = "helix-foundation"
version = "0.1.0"
description = "Helix three-role local control-plane prototype"
requires-python = ">=3.11"
[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
